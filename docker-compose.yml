version: '3.8'

services:
  tts-studio:
    build: .
    container_name: tts-studio
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
    volumes:
      - ./outputs:/app/outputs
